# ADR-005: DTCG export deferred

Status: deferred for Spike 1.

No named consumer, importer, or authorized interchange workflow exists for this disposable harness. Generating decorative JSON would add an unsupported contract, so the runtime candidate remains generated CSS only. Revisit DTCG only when a concrete consumer and round-trip acceptance test are authorized.

---
title: Start
description: Opt in to a small, reset-free design language.
hero:
  title: Small CSS. Clear boundaries.
  tagline: Neobrui gives native HTML a tactile visual grammar without taking over the product.
---

## Opt in, then stay native

Neobrui is deliberately scoped. Import the package, add an explicit class or theme scope, and remove either without changing semantics or behavior.

<div class="nbr-demo nbr-stack">
  <h2>Neutral playground</h2>
  <p>Product identity belongs to the product. This example uses semantic roles and native controls.</p>
  <div class="nbr-cluster">
    <button class="nbr-pressable" type="button">Try an action</button>
    <a class="nbr-pressable" href="/neobrui/principles/">Read principles</a>
  </div>
</div>

## Live product mapping

A product scope can map the same roles to its own palette while the markup stays unchanged. See [Foundations](/neobrui/foundations/) for the small set of tunable defaults.

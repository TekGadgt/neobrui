---
title: Patterns
description: Recipes for asymmetric, native HTML interfaces.
---

Patterns are copyable recipes, not package APIs. Each recipe keeps behavior with native HTML and lets the product own content, identity, and application state.

<div class="nbr-gallery" data-specimen="neutral">
  <article class="nbr-demo nbr-surface" data-nbr-level="raised"><p class="eyebrow">Featured / 01</p><h2>Editorial lead</h2><p>Give the primary story more space, then lead into evidence.</p><a href="#recipe">Read the case study</a></article>
  <aside class="nbr-demo nbr-surface" data-nbr-level="outlined"><h3>Callout</h3><p>Pair tone with a textual label; urgency belongs to the application.</p></aside>
  <div class="nbr-demo"><h3>Badge</h3><p><span class="nbr-pressable">In progress</span> is not color-only status.</p></div>
</div>

<section class="nbr-demo nbr-surface" data-specimen="product" data-nbr-example-theme="product" aria-label="Product identity: Coral Ledger"><h2>Product mapping</h2><p>Product-owned identity can map tokens without changing native semantics.</p><button type="button">Try the pattern</button></section>

## Recipe set

Cards, callouts, badges, navigation/toolbars, editorial sections, heroes, and mixed-span galleries all follow the same contract: purpose, anatomy, good/bad comparison, responsive behavior, native semantics, applicable states, theming hooks, and a removal note.

<section id="recipe"><h2>Removal note</h2><p>Remove the Neobrui classes and role mapping; the headings, links, and content still work as semantic HTML.</p></section>

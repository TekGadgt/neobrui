{
  "name": "neobrui-spike-0-harness",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.21.0",
  "type": "module",
  "scripts": {
    "install:frozen": "pnpm install --frozen-lockfile",
    "build": "vite build",
    "test": "playwright test",
    "verify": "pnpm install --frozen-lockfile && pnpm build && pnpm test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "vite": "^7.1.7"
  }
}
